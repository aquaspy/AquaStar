package Network {
    /**
     * Deliberately inert: ServerSocket is Adobe AIR-only and cannot be linked
     * into AquaStar's PPAPI Flash runtime.
     */
    public class TCP {
        public function TCP() {}
        public function init():void {}
    }
}
